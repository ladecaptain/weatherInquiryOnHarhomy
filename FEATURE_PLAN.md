# 停云见晴 — 功能拓展方案

> 基于当前版本 (v1.0.0) 分析，制定分阶段功能拓展路线图。

---

## 现有功能总览

### 🌤 天气数据
| 模块 | 详情 |
|---|---|
| 实时天气 | 天气图标(SVG)·温度·体感温度·天气描述·湿度·风向·风力 |
| 逐小时预报 | 横向滑动 24h，温度·降水概率·风向风力 |
| 7 天预报 | 日期标签·温差可视化指示条·日出日落·月相 |
| 环境数据 | AQI(国标计算)·紫外线·湿度·气压·能见度·风力，6 格布局 |
| 空气质量 | 内置 HJ 633-2012 AQI 计算引擎，API 不可用时降级本地计算 |

### 🏙 多城市管理
| 模块 | 详情 |
|---|---|
| Swiper 滑动 | 左右切换城市，底部圆点指示器 |
| 自动定位 | GPS 单次+持续监听，Haversine 5 km 阈值自动切换 |
| 收藏管理 | 城市搜索·添加·删除，右滑删除，Preferences 持久化 |
| 降级策略 | 定位失败→默认北京，API 失败→静默降级 |

### 🎨 视觉体验
| 模块 | 详情 |
|---|---|
| 天气背景 | 10 种主题(晴/雨/雪/雷/雾/沙/风/多云/夜间/默认)，昼夜自动切换 |
| 下拉刷新 | 自定义指示器，深色毛玻璃风格，SafeArea 沉浸式适配 |

### 🏗 技术架构
- **状态管理**: `@ObservedV2` + `@Trace` 属性级精确更新
- **API 层**: QWeather 6 个端点（15 天预报 URL 已定义未使用）
- **权限**: `LOCATION` + `APPROXIMATELY_LOCATION` + `INTERNET`
- **SDK**: HarmonyOS Next API 24 (6.1.1)

---

## 🥇 第一优先级 — 补齐核心体验

### ⚡ 天气预警
- **目标**: 对接 QWeather `/v7/warning/now` API
- **需求**:
  - 数据模型：`WarningData`（预警类型、等级、标题、详情、发布时间）
  - API 函数：`getWeatherWarning(locationId: string)`
  - UI 组件：`WarningBanner` 首页顶部横幅，四级颜色（蓝/黄/橙/红）
  - 点击展开详情弹窗，显示预警全文 + 防御指引
- **工作量**: 中

### 📅 15 天预报
- **目标**: 激活已定义的 `FORECAST_15D_URL` 端点
- **需求**:
  - API 函数：`getForecast15D(locationId: string)`
  - 在 `ForecastList` 组件中新增 "7 天 / 15 天" 切换 Tab
  - 复用现有温差指示条，适配更长时间范围
- **工作量**: 小

### 🌡 温度单位切换
- **目标**: °C ↔ °F 自由切换，Preferences 持久化偏好
- **需求**:
  - 工具函数：`convertTemp(celsius: string, unit: TempUnit): string`
  - 设置存储：`Preferences` 存取 `temp_unit` 偏好
  - UI 入口：设置齿轮图标 → 单位切换弹窗
  - 全局刷新：切换后所有温度显示同步更新
- **工作量**: 小

### 🔔 预警推送通知
- **目标**: 恶劣天气主动通知用户
- **需求**:
  - `@kit.NotificationKit` 本地通知
  - `WorkScheduler` 后台定时轮询（每 30 分钟）
  - 检测到预警后推送通知，点击跳转 App 首页
- **工作量**: 中

---

## 🥈 第二优先级 — 增强交互体验

### 📱 桌面卡片 (Widget)
- **目标**: `FormExtensionAbility` 实现系统桌面天气卡片
- **需求**: 2×2/4×4 两种尺寸，显示当前温度·天气图标·城市名，点击进入 App
- **工作量**: 中

### 🗄 离线缓存
- **目标**: 无网络时展示上次缓存的天气数据
- **需求**: `@kit.ArkData` RDB 存储，TTL 30 分钟过期，离线提示 "数据已过期 XX 分钟"
- **工作量**: 中

### 🌐 多语言 (i18n)
- **目标**: 补齐 `$r()` 资源引用，支持中英文切换
- **需求**: 消除组件内硬编码中文，添加 `en_US` 资源文件，跟随系统语言
- **工作量**: 中

### ❌ 错误状态优化
- **目标**: 用户友好的错误反馈
- **需求**: 网络异常提示·重试按钮·骨架屏加载态·非静默 API 错误
- **工作量**: 小

---

## 🥉 第三优先级 — 深度功能拓展

### 📊 城市详情页
- **目标**: 新增 `pages/CityDetail` 页面，展示该城市完整天气数据
- **需求**: 15 天趋势折线图·日出日落日历·月相时间线·历史统计
- **工作量**: 大

### 🎤 语音搜索
- **目标**: `@kit.IntelligenceKit` 语音识别输入城市名
- **需求**: 搜索页添加麦克风按钮，语音→文本→自动搜索
- **工作量**: 中

### 📤 天气分享
- **目标**: 生成天气卡片截图并通过系统分享面板发送
- **需求**: 截图生成·分享意图·支持微信/短信/保存相册
- **工作量**: 中

### 🌍 天气地图
- **目标**: `@kit.MapKit` + QWeather 天气图层
- **需求**: 地图上叠加降水/温度/云图图层，可视化区域天气
- **工作量**: 大

### 🔄 设备协同
- **目标**: `@kit.DistributedServiceKit` 跨设备同步
- **需求**: 手机↔平板收藏城市自动同步，分布式数据对象
- **工作量**: 大

---

## 🛠 工程化提升

| 功能 | 说明 | 工作量 |
|---|---|---|
| 🧪 单元测试 | 替换占位测试，AqiCalculator / WeatherUtils / FavoriteCityManager hypium 用例 | 小 |
| 📋 Mock 数据 | 填充 `mock-config.json5`，无 API 配额离线开发 | 小 |
| 💾 备份恢复 | 完善 `EntryBackupAbility`，收藏数据跟随华为账号迁移 | 小 |
| ♻ 组件复用 | `SearchBar` 组件已定义未使用，重构 SearchPage 引用它 | 小 |

---

## 实施路线图

```
第 1 周  ████████ 天气预警 + 温度单位切换 + 15 天预报
第 2 周  ████████ 离线缓存 + i18n + 错误状态优化  
第 3-4 周 ████████ Widget 桌面卡片 + 通知推送 + 城市详情页
第 5 周+  ████████ 语音搜索 + 天气分享 + 单元测试
远期       ████████ 天气地图 + 设备协同 + 备份恢复
```

---

*文档生成时间: 2026-07-01*
